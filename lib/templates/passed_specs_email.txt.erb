*<%= app_name %>*

Specs passed on <%= run.branch %>!

Next steps (to deploy on master):
  $ git fetch
  $ git checkout <%= run.branch %>
  $ git pull --rebase origin master

If there were no changes, merge into master to trigger a deploy!
  $ git checkout master<br />
  $ git rebase <%= run.branch %><br />
  $ git push
